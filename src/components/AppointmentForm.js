import React from "react";

export const AppointmentForm = () => {
  return (
    <form id="appointment">
      <select name="service" id="service">
        <option />
      </select>
    </form>
  );
};
